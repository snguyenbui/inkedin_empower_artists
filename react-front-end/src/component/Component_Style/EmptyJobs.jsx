import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  buttonContainer: {
    padding: "10px",
  },
  button: {
    width: "7%",
    height: "auto",
    color: "lavender",
  },
});

export { useStyles };
